
#ifndef OSPREY_FORMAT_HPP
#define OSPREY_FORMAT_HPP

#include <string>

extern std::string format(const std::string& format, ...);
extern std::string formatVector(const std::vector<int>& v);

#endif
